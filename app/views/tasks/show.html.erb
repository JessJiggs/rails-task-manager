<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<br>

<%= link_to "Update Task", edit_task_path(@task)%>


<%= link_to "Delete", task_path(@task), #not need destroy_task_path -
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
